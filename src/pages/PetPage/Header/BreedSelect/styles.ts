import styled from "styled-components";

export const Select = styled.select`
    min-width: 56rem;
    height: 100%;
    margin: 0 4rem;
    padding: 0 2rem;

    background-color: #FFEED8;

    border: 0px;
    border-radius: 4px;

    cursor: pointer;

    option {
        color: black;
        display: flex;
        white-space: pre;
        height: 20px;
        padding: 0px 2px 1px;
    }
`;